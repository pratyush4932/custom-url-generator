<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300;1,400;1,500&display=swap"
        rel="stylesheet">
    <link rel="icon" href="../public/assets/url_8257840.ico" type="image/x-icon">
    <link rel="stylesheet" href="../public/css/style.css">
    <title>Url Shortener</title>
</head>

<body>
    <h1>Url Shortener</h1>
    <div class="underText">The custom url entered should be unique and should not contain any special characters..</div>
    <div class="form-section">
        <form action="/" method="POST">
            <div class="input-area">
                <input class="intputBox" type="text" name="longUrl" value="<%=userlongUrl%>" placeholder="Enter URL" autocomplete="off"
                    autofocus required>
            </div>
            <div class="input-area">
                <input class="intputBox" type="text" name="custom_name" value="<%=userCustom_name%>" placeholder="Enter custom name"
                    autocomplete="off" autofocus required>
            </div>
            <button type="submit">Generate Short URL</button>
        </form>
    </div>

    <div class="result-section">
        <% if(custom_link !=="" ){ %>
            <p>Your Custom Link : <a href="<%=custom_link%>">
                    <%=custom_link%>
                </a></p>
            <% }else{ %>
                <p><strong>Unknown_Error: </strong>The custom name is already used.Please use a different keyword!</p>
                <% }%>
    </div>
</body>

</html>